#!/usr/bin/env ruby

require 'optparse'
require 'puppet_metadata_checker'

options = {}
OptionParser.new do |opts|
  opts.on("-v", "--[no-]verbose", "Run verbosely") do |v|
    options[:verbose] = v
  end
end.parse!

PuppetMetadataChecker::Runner.run(ARGV, options[:verbose])
